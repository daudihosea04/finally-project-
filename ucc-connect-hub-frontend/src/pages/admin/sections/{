const SecurityComplianceSection = () = const { colors } = useTheme(); return <div className="glass-card p-6" style={{ border: `1px solid ${colors.border}` }}><h2 className="text-xl font-bold" style={{ color: colors.textPrimary }}>Security 
